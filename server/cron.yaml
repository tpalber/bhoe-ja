cron:
  - description: "Scrape articles every hour"
    url: /api/scrape-articles/all
    schedule: every 1 hours
  - description: "Scrape videos every 2 hours"
    url: /api/scrape-videos
    schedule: every 2 hours
